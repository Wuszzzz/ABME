# Integrations

This directory documents public-safe connection patterns between ABME and execution capabilities.

- CLI: command-line tools that provide deterministic local or remote actions.
- MCP: tool servers that expose external systems through a structured interface.

Each integration document should state its purpose, prerequisites, input/output boundaries, example workflow, and material that must not be committed.
