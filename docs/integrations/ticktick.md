# TickTick 接入说明

## 当前状态

TickTick 不是当前 `abme` 的主链路。

当前项目主链路是：

- 思源作为资源层
- `abme` 作为调试工作台
- 飞书角色 agent 作为运行层

因此，TickTick 目前只适合作为未来可选扩展，不再作为核心架构前提。

## 可选用途

- 把调试待办同步成任务
- 给发布前检查和回归检查建清单
- 给问题单维护轻量状态

## 当前原则

- 可以保留为可选集成
- 不再作为项目主叙事
- 不应替代思源中的项目记录和调试沉淀
