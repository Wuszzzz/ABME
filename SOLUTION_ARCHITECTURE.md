# ABME Agent 调试工作台整体架构

## 1. 定位

`abme` 不是一个先做 MVP 再补能力的终端产品，而是一个长期使用的 agent 调试工作台。

它承担三类核心工作：

- 调试主入口 `J.A.R.V.I.S.` 与各主合伙人、子 agent 的提示词
- 联调角色之间的调用关系、升级机制、回写机制与发布前检查
- 记录每日调试、实验、故障、复盘，并把有效结论沉淀进思源

因此，`abme` 的目标不是“替代思源”或“替代飞书”，而是作为中间调试层，把资源层和运行层连起来。

## 2. 三层模型

### 2.1 资源层：思源笔记

思源是整个 second brain 的资源方和知识真源。

- `00-Core`：程序性记忆，放规则、角色、路由、方法
- `01-Profile`：自我模型，放偏好、目标、风格、决策标准
- `10-Projects`：项目层，放正在推进的事项
- `20-Knowledge`：结构化知识层
- `30-Self Inbox`：自己的原始输入
- `40-External Inbox`：外部原始输入
- `50-Outputs`：成品输出
- `90-Archive`：历史归档

对 agent 系统来说，思源负责三件事：

- 提供稳定上下文
- 承接调试记录与项目记录
- 接收已验证结论的正式回写

### 2.2 调试层：ABME

`abme` 是本地调试工作台，不是运行层机器人本身。

它的职责是：

- 维护系统级文档、规则文档、接入规范和调试方法
- 调试提示词、角色边界、调用链和工作流
- 做实验设计、问题复现、对比分析、回归检查
- 组织每天的调试日志、问题单、发布说明、复盘结论
- 把需要长期保留的结果同步到思源指定位置

`abme` 对思源拥有最高写权限，因为它负责调整系统结构本身。

### 2.3 运行层：飞书角色 Agent

飞书中的各角色 agent 是前台运行层。

它们的职责是：

- 承接真实对话与真实任务
- 按角色边界执行具体工作流
- 产生运行反馈、异常、争议点和复盘材料
- 将需要沉淀的内容回流给思源

飞书层不应该成为规则真源，也不应该独立维护一套知识库。它的稳定规则、提示词版本和协作边界，仍然应回到思源和 `abme` 管理。

## 3. 两个调用层的关系

当前系统的两个调用层是：

- `abme`：调试、设计、复盘、发布前检查
- 飞书角色 agent：实际运行、协作、执行、反馈

二者共用同一个资源层，也就是思源。

区别在于：

- `abme` 负责改系统
- 飞书 agent 负责跑系统

因此，权限边界建议如下：

- `abme`：可读写思源全部核心区域，尤其是 `00-Core`、`01-Profile`、`10-Projects`
- 飞书 agent：默认只读规则和知识，写入以运行记录、项目反馈、复盘摘要为主

## 4. ABME 的日常工作流

### 4.1 调试前

- 从思源读取当前规则、角色定义、项目上下文和历史问题
- 明确本次调试目标：是提示词问题、路由问题、工作流问题，还是运行反馈问题

### 4.2 调试中

- 在 `abme` 中完成提示词修改、结构调整、链路检查和实验对比
- 对关键变化进行记录，包括旧问题、改动点、实验结果和风险

### 4.3 调试后

- 将本次调试记录写回思源 `11-Agent Debug`
- 如果结论已经稳定，再将规则升级写回 `00-Core` 或 `01-Profile`
- 如果只是经验、方法或故障模式，沉淀到 `20-Knowledge`

## 5. 飞书运行层的回流机制

飞书角色 agent 的运行结果，不应该只留在群聊里。

需要至少回流三类信息：

- 运行异常：角色失配、权限冲突、链路断裂、提示词不稳定
- 调试线索：用户真实反馈、失败样本、误判案例
- 运行总结：哪些工作流稳定、哪些还需要 `abme` 继续调

这些内容优先回写到：

- `11-Agent Debug`
- 具体业务项目页
- 已经被验证有效的方法页

## 6. Agent 调试工程

思源中需要有一个专门项目，作为调试工作的承接层：

- 笔记本名称：`11-Agent Debug`
- 笔记本位置：独立调试笔记本

建议在该项目下至少保留这些文档：

- `README-Agent-调试工程-项目说明`
- `提示词调试记录`
- `工作流联调记录`
- `飞书角色运行复盘`
- `问题单与修复记录`

这个项目不负责保存最终规则真源，而是负责承接所有正在发生的调试工作。

## 7. 仓库内文档职责

`abme` 仓库当前更适合承接这些内容：

- 架构文档
- 调试规范
- 接入规范
- 工作台使用说明
- 图示和模板

不适合把它继续写成一个“只读思源 + 读写滴答”的 MVP 产品方案。

## 8. 决策原则

1. 思源是资源层，不是附属文档仓库。
2. `abme` 是调试工作台，不是运行层机器人。
3. 飞书角色 agent 是运行层，不是规则真源。
4. 调试记录先进入 `11-Agent Debug`，稳定后再升级到 `00-Core`、`01-Profile` 或 `20-Knowledge`。
5. 前台运行反馈必须能回流到思源，而不是停留在聊天记录里。

## 9. 当前阶段的正确目标

现阶段不应该再围绕“先做一个 MVP CLI 工具”来组织项目。

当前更合理的目标是：

- 先把资源层、调试层、运行层的边界定清楚
- 把提示词调试和复盘机制跑顺
- 把思源中的项目承接层建好
- 再逐步补自动化脚本和发布链路
